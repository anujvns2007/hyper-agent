# MacBook — copy to .env next to docker-compose.local.yaml
#   cp .env.example .env
#
# Parent of your repos (for serena multi-project). Use a single repo path for code-graph/knowledge-rag.
PROJECT_ROOT=/Users/you/work/repo

# GPU host — optional .env next to docker-compose.remote.yaml
# DOCS_SYNC_INTERVAL_SECONDS=86400
